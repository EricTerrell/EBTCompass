<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ericbt.ebtcompass">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.sensor.compass"
        />

    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        />

    <uses-feature
        android:name="android.hardware.location.gps"
        />

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EBTCompass"
        >

        <service
            android:name="com.ericbt.ebtcompass.services.CompassService"
            android:enabled="true"
            android:exported="false"
            />

        <service
            android:name="com.ericbt.ebtcompass.services.GPSService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            />

        <activity
            android:name="com.ericbt.ebtcompass.activities.MainActivity"
            android:launchMode="singleInstance"
            android:configChanges="orientation|screenSize"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.ericbt.ebtcompass.activities.GoLineActivity"
            android:label="@string/title_activity_line"
            />

        <activity
            android:name="com.ericbt.ebtcompass.activities.GoToPointActivity"
            android:label="@string/title_activity_point"
            />

        <activity
            android:name="com.ericbt.ebtcompass.activities.AboutActivity"
            android:label="@string/title_activity_about"
            />

        <activity
            android:name="com.ericbt.ebtcompass.activities.SettingsActivity"
            android:label="@string/title_activity_settings"
            />

        <activity
            android:name="com.ericbt.ebtcompass.activities.LicenseTermsActivity"
            android:label="@string/title_activity_license_terms"
            />

    </application>

</manifest>